# Environment Configuration

# Server Configuration
PORT=8080
HOST=0.0.0.0
# Set to 'development' for local dev or 'production' for deployment
NODE_ENV=production

# For production deployment (uncomment and update)
FRONTEND_URL=https://atomixtools.com
FRONTEND_URL_WWW=https://www.atomixtools.com

# CORS Configuration - Environment-based origin handling
# DEVELOPMENT MODE (NODE_ENV=development):
#   - Automatically allows: http://localhost:3000, http://localhost:3001, http://127.0.0.1:3000
#   - Plus any additional origins from ALLOWED_ORIGINS below
#
# PRODUCTION MODE (NODE_ENV=production):
#   - Strictly allows only: https://atomixtools.com, https://www.atomixtools.com
#   - Plus any additional production origins from ALLOWED_ORIGINS below
#   - NEVER use wildcards (*) in production
#
# Additional allowed origins (comma-separated, no spaces):
# For staging/testing: https://staging.atomixtools.com,https://test.atomixtools.com
ALLOWED_ORIGINS=
# IMPORTANT: ALLOWED_ORIGINS is OPTIONAL and adds to the default allowed origins based on NODE_ENV

# Logging
LOG_LEVEL=info
